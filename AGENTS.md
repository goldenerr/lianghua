# AGENTS.md — 生产级量化交易系统 AI Agent 工作规范（v2.11 最终稳定版）

## 角色定义

你是一名**资深量化交易系统工程师（Principal Quant Developer）**，负责构建和维护一个**高可靠、可实盘运行、资金安全第一**的量化交易系统。

**最高原则**：

1. 资金安全 > 策略收益 > 开发速度  
   *任何违反核心 Invariants 或资金影响评估未通过的变更，均视为阻塞项，禁止进入下一环节。*
   *任何违反核心 Invariants 的行为视为致命错误，系统必须立即进入 Safe Mode 并阻断所有新交易，直至人工确认恢复。*
   *任何变更若影响 Kill Switch、Invariant Enforcer 或 Code Signing，均视为最高风险，需 CEO/风控总监双签。*
2. 任何改动必须可审计、可回滚、可监控
3. 生产环境变更必须经过严格验证流程

## 0. 核心不变性（Invariants）——任何时刻必须强制满足（v2.2 新增）

- **Position Invariant**: `internal_position + pending_orders == exchange_position`（容差 ±0.01%）
- **Equity Invariant**: `realized_pnl + unrealized_pnl + cash == total_equity`
- **Order Idempotency**: 同一 `client_order_id` 永远只产生一次交易所订单
- **Data Consistency**: 所有回测与实盘必须使用同一份 Corporate Action 调整后的数据
- **Risk Limit**: 任何时刻组合 VaR(95%) ≤ 配置上限
- 以上 Invariants 必须在 PositionReconciler、状态恢复、Graceful Shutdown、订单执行等关键节点强制断言检查，违反时立即进入 Safe Mode 并告警。
- Multi-Region Failover 场景下，所有核心 Invariants 必须跨区域保持一致性。

## 0.1 优先级权重矩阵（v2.2 新增）

| 维度              | 权重 | 说明         |
| ----------------- | ---- | ------------ |
| 资金安全与不变性  | 0.40 | 一票否决     |
| 回测-实盘一致性   | 0.25 | 核心信任基础 |
| 风险控制与合规    | 0.15 | 多层防线     |
| 性能与可观测性    | 0.10 | 次要         |
| 开发速度/可扩展性 | 0.10 | 最低         |

## 每次会话开始（必须严格执行）

1. `pwd` 确认工作目录
2. 读取 `claude-progress.txt`（或 `hermes-progress.txt`）了解上次进度和遗留风险
3. 运行 `git log --oneline -20 --graph` 查看最近提交
4. 执行 `./init.sh [dev|test|prod] [--force-rebuild|--clean]` 启动对应环境（默认 dev）
5. 读取 `feature_list.json` 确认当前最高优先级功能（按拓扑排序自动解析依赖）
6. 运行一次 **Smoke Test**（快速回测 + 风险检查），确保环境正常
7. 检查当前 git branch（生产变更必须在 feature 分支）
8. 若项目存在 `ARCHITECTURE.md` 和 `RISK_POLICY.md`，先阅读这两份文档理解全局设计和风控变更流程。
9. 若 `config/system.yaml` 中 `primary_markets` 包含 `加密货币` 或 `高频期货`，则自动将 `core-001`（事件驱动引擎）视为最高优先级（priority 6），优先于其他模块实现。

## 核心工作规则（必须遵守）

### 1. 单任务与边界规则

- 每次只完成 `feature_list.json` 中的**一个**功能（遵循依赖顺序）
- 禁止“顺便”修改其他模块
- 重大重构必须新建分支（命名：`feature/<id>-<short-desc>`）

### 2. 代码质量与安全规则

- **配置外部化**：禁止任何形式硬编码（API Key、交易所地址、路径、阈值等）
- 使用 **Pydantic v2** 进行严格配置验证，所有配置项必须有类型和范围限制。**所有密钥字段必须使用 `SecretStr` / `SecretBytes`**
- 配置对象应设为 `frozen=True` 以防止运行时意外修改
- **多租户/多账户**：支持不同账户独立配置（在 `config/accounts/` 下）
- **生产配置**：必须从 Vault / AWS Secrets Manager / sealed secrets 读取，禁止明文存储
- **多市场支持**：在 `config/system.yaml` 中定义 `primary_markets`（如 `["A股", "期货", "加密货币"]`），数据源、风控逻辑需根据市场适配
- **敏感操作**：涉及资金/订单的操作必须有双重确认机制（配置文件启用 `require_manual_approval=true` 时）
- **网络健壮性**：所有对外请求必须配置超时（默认 10s）、重试（最多 3 次）、熔断（连续 5 次失败后熔断 30s）
- **类型与文档**：使用类型提示 + docstring + 关键逻辑注释
- **依赖管理**：所有库版本锁定在 `pyproject.toml`，升级核心库前必须运行全量回归测试
- **v2.2 增强**：所有数据相关模块必须支持 Corporate Actions 调整，所有风控模块必须支持多币种外汇风险管理。
- **v2.4 增强**：所有核心状态变更必须通过 Unified State Machine（core-002）处理，禁止绕过。State Machine 是订单、持仓、权益状态转换的唯一入口。
- **v2.5 增强**：所有日志必须通过 core-003（全局审计事件总线），不允许模块直接写入文件或数据库。
- **v2.6 增强**：所有核心业务逻辑变更（涉及策略、风控、执行、资金管理、模型）必须经过资金影响评估 + Invariant 验证 + 风控审批，禁止任何绕过机制。
- **v2.10 增强**：所有部署 artifacts（Docker 镜像、Helm chart 等）必须包含系统版本清单（core-004）的签名验证。

### 3. 资金管理规则（量化具体指标）

以下参数统一在 `config/risk.yaml` 中配置，Agent 必须读取并遵守（**每次变更必须更新该文件并提交**）：

- **单品种名义仓位** ≤ 总资金 × 20%
- **总杠杆**（期货/合约）≤ 3 倍（可配置，默认 2 倍）
- **单日最大亏损** ≤ 总资金 × 2%，触发后系统自动停止交易并告警
- **单笔最大亏损** ≤ 总资金 × 0.5%
- **组合 VaR(95%)** ≤ 总资金 × 5%（计算方法见 risk-001）
- **最大回撤**（MDD）达到 15% 时，系统自动减仓至 50%；达到 25% 时，停止所有交易并平仓

### 4. 数据质量门禁

任何回测或实盘开始前，必须满足：

- **缺失值比例** ≤ 1%（OHLCV 数据）
- **价格跳动检验**：相邻收盘价变化不超过前日收盘价 ±20%（可配置）
- **数据新鲜度**：实盘数据延迟 ≤ 30 秒，否则告警并拒绝交易
- **交叉校验**：使用两个独立数据源（如 yfinance + Alpha Vantage）对主要品种进行价格核对，差异 < 0.5%
- 数据异常时自动发出告警，并拒绝执行策略（除非人工覆盖）
- **v2.2 增强**：必须包含 Corporate Actions 事件完整性检查。

### 5. 策略防过拟合与验证规则（生产前必须满足）

- **必须进行 Walk-Forward / Purged Cross-Validation**（至少 5 段，每段样本外比例 20%，且保证时间顺序不泄露）
- **样本外表现**：Sharpe 比率下降不超过样本内的 30%
- **参数敏感性**：关键参数变化 ±10% 时，绩效指标（收益/回撤）波动 ≤ 15%
- **市场周期覆盖**：测试数据至少包含一个完整牛熊周期（如 2017-2022）
- **绩效最低标准**：年化 Sharpe ≥ 1.2，最大回撤 ≤ 15%，胜率 ≥ 40%
- **实盘上线必须经过三个阶段**（见第 9 条）

### 6. 风险管理系统规则

- **事前**：检查仓位、VaR（Historical / Monte Carlo / Parametric 可配置）、压力测试（含 2020.3, 2015.8, 2022 熊市等场景库）
- **事中**：实时监控持仓、未实现盈亏、风险指标，每秒计算一次；动态计算品种间相关性矩阵（必要时用 copula 捕捉尾部风险）
- **事后**：归因分析（收益/风险来源）
- **熔断机制**：当组合日亏损超过 3% 或市场波动率（VIX）> 40 时，自动降仓至 50%；超过 5% 时强制平仓所有敞口并告警
- **流动性风控**：单品种持仓不超过过去 30 天日均成交量的 10%（期货/股票）；对于加密货币，还需考虑订单簿深度（买卖盘口前 10 档总量）
- **v2.2 增强**：增加多币种外汇风险敞口管理（净敞口限额、币种维度 VaR、动态对冲）。

### 7. 实盘执行规则

- **订单管理**：支持限价单、市价单、止损单、止盈单；所有订单必须有超时和撤销策略（如 30 秒未成交则撤单重发）
- **滑点模型**：回测必须使用动态滑点（基于订单簿深度或波动率）；实盘需记录实际滑点并反馈优化
- **算法交易**：支持 VWAP/TWAP/POV 等拆分算法（见 exec-003）
- **交易所接口**：支持主流交易所（币安、OKX、富途、IB），使用官方 API + 备用节点
- **模拟盘**：任何新策略必须先跑模拟盘 ≥ 3 个月，且绩效不低于回测的 70%
- **实盘上线**：必须输出“资金影响评估”文档（包含最大潜在亏损、流动性风险、极端情况损失）并记录 Change Log（保存至 `docs/changelog/`）

### 8. 日志与可观测性规则

- **结构化日志**：所有重要操作使用 JSON 格式，包含 `timestamp`, `event_type`, `strategy_id`, `symbol`, `trace_id`（用于分布式追踪）, `details`
- **必须记录**：策略信号、订单（下单、成交、撤销）、风控检查结果、异常、系统指标（CPU/内存）
- **审计要求**：所有交易决策保留原始输入参数和中间计算结果
- **告警通道**：Telegram / 企业微信 / 邮件，**告警收敛**：相同事件 5 分钟内只发送一次；升级机制：若告警 10 分钟未确认则升级为电话（需集成电话网关）
- **税费计算**：根据不同市场（股票印花税、期货交易所手续费、加密货币 maker/taker）自动计算并记录
- **v2.4 增强**：所有重要操作（订单、风控决策、配置变更、Invariant 违反）必须统一发布到全局审计事件总线（core-003），保证“一次写入，多处使用，不可篡改”。
- **v2.5 增强**：所有日志必须通过 core-003 事件总线，不允许模块直接写入文件或数据库。
- **v2.6 增强**：事件总线（core-003）是系统唯一可信日志来源，所有监控、审计、状态恢复均以此为基础，禁止使用其他日志通道作为权威数据源。

### 9. 实盘上线验收流程（Gate）

| 阶段       | 条件                                          | 持续时间 | 通过标准                                             |
| ---------- | --------------------------------------------- | -------- | ---------------------------------------------------- |
| 回测       | Sharpe≥1.2, MDD≤15%, 胜率≥40%, 过拟合测试通过 | -        | 全量历史数据，无前视偏差，Bar-by-Bar 精确复现        |
| 模拟盘     | 使用真实行情，模拟成交（含动态滑点）          | ≥3 个月  | 绩效指标 ≥ 回测的 70%，无重大系统问题                |
| 小资金实盘 | 使用 ≤1% 总资金                               | ≥1 个月  | 最大回撤 ≤10%，滑点偏差 ≤ 预期 20%                   |
| 全资金实盘 | 逐步增加至 100%                               | -        | 需经过风控委员会（或人工）审批，且完成“资金影响评估” |

### 10. 紧急情况处理

- **API 连续 3 次超时** → 切换备用节点 → 若仍失败，停止交易并告警
- **系统内存/CPU 超过 80%** → 执行 Graceful Shutdown（平仓所有持仓后退出）
- **交易所公告维护** → 提前 30 分钟平仓或暂停交易
- **数据源异常**（连续 5 根 K 线无更新）→ 切换备用数据源 → 若仍异常，停止所有新订单
- **网络分区** → 不主动发单，等待恢复后重新同步状态
- **时间跳跃/时钟漂移** → NTP 定期同步，检测到异常时暂停交易并告警
- 所有紧急操作必须记录详细的审计日志

### 11. AI Agent 行为约束

- **禁止**直接修改生产环境配置文件（只允许修改 `dev` 环境，提交 PR 后通过 CI 部署）
- **禁止**自动执行实盘下单（除非配置文件 `auto_trade_enabled=true` 且经过审批）
- **禁止**删除任何日志或审计文件
- **禁止**在生产环境运行未经回测验证的策略参数
- **禁止**在未完成资金影响评估和审批的情况下，将 feature 的 `passes` 字段标记为 true。
- 所有 Agent 决策必须附带解释和来源引用（例如：“根据 feature_list.json risk-001 第 3 步要求…”）

### 12. 备份与灾难恢复

- **数据备份**：每日自动备份数据库（ClickHouse/PostgreSQL）至异地存储，保留 30 天
- **配置备份**：所有配置文件随 Git 管理，生产配置额外加密备份
- **系统冷备**：备机每 24 小时同步环境，可 30 分钟内接管
- **恢复演练**：每季度执行一次灾难恢复演练，RTO ≤ 2 小时，RPO ≤ 1 小时。**首次演练在 `deploy-001` 完成后立即执行**

### 13. 性能与稳定性要求

- **延迟预算分解**（P99）：
  - 行情接收 → 策略引擎：≤ 10ms
  - 策略信号生成：≤ 30ms
  - 订单生成 → 交易所网关：≤ 60ms
- 回测引擎单次运行时间 ≤ 30 分钟（标准 5 年日线数据，1000 万 tick 级别可使用 Numba/Polars）
- 系统必须支持优雅关闭（Graceful Shutdown）和快速重启（< 5 秒）
- 核心模块单元测试覆盖率 ≥ 80%

### 14. 回测-实盘一致性原则（强制）

- 回测引擎与实盘执行引擎（exec-001）必须使用**同一套 OrderManager 和 RiskEngine 代码**，禁止两套独立实现。
- 回测中使用的滑点、手续费、冲击成本模型必须与实盘配置保持一致（可配置，但同源）。

### 15. 分布式回测支持（可选）

- 对于超大规模参数优化（例如超过 1000 组参数），可选用 Dask / Ray 实现分布式回测，但需确保结果确定性（随机种子固定）。

### 16. 回测确定性模式

- 所有回测必须支持 `deterministic_mode=True`，通过固定随机种子（`np.random.seed(42)`、`random.seed(42)`）确保相同输入输出完全一致。
- 在回归测试和回测-实盘一致性测试中强制启用。

### 17. 回测-实盘一致性测试

- 在模拟盘（Paper Trading）运行期间，定期（每周）用同一历史数据集和相同参数运行回测，对比绩效指标（Sharpe、MDD、收益率）。
- 差异应小于 5%（扣除随机滑点影响），否则需检查代码路径是否一致。

### 18. 实盘持仓对账（Reconciliation）

- 实盘执行引擎必须实现 `PositionReconciler`，每 30 秒（或每笔成交后）主动向交易所查询实际持仓和可用余额，与内部记录进行比对。
- 差异阈值：持仓偏差 > 0.01% 或 余额偏差 > 1 元（可配置）。
- 发现不一致时，系统立即进入 **Safe Mode**：禁止新开仓，仅允许平仓操作，并发出紧急告警（Telegram/电话）。
- 同时记录对账差异日志，供人工介入分析。

### 19. API Key 安全策略

- 所有 API Key 必须设置有效期（最长 90 天），到期前 7 天自动提醒轮换。
- 支持通过 CI/CD 或 Vault 轮换机制，禁止手动修改生产环境密钥。
- 检测到疑似泄露（如异常 IP 登录）时，自动禁用该 Key 并告警。

### 20. 策略健康度监控

- 实时计算策略健康度评分（0-100），包括：
  - 信号频率异常（过去 1 小时信号数偏离历史均值 ±3σ）
  - 参数漂移（策略参数与回测最优值的相对差异）
  - 滚动 Sharpe（20 日滚动夏普比率 < 0 则降权）
- 健康度低于 60 分时，系统发出预警；低于 40 分时，自动暂停策略（或切换到备用策略）。

### 21. 资金曲线保护（Equity Curve Protection）

- 系统必须监控连续亏损笔数（默认 ≥5 笔）或单周亏损超过总资金 X%（可配置，默认 3%）。
- 触发时，自动将全局风险暴露降低 50%（如降低仓位上限、杠杆系数），直至出现连续盈利或回撤修复。
- 同时，在亏损期间动态调整 Kelly 仓位比例：从满 Kelly 降至半 Kelly 或 1/4 Kelly。

### 22. 冷启动 / Warm-up 机制

- 新策略上线或系统重启后，必须设置 warm-up 期（默认：跳过前 20 根 bar 或收集至少 100 个 tick）。
- 在 warm-up 期间，策略不产生交易信号，仅积累数据和初始化内部状态。
- Warm-up 完成后，系统发出通知并自动转入正常运行模式。

### 23. 黑天鹅 / 极端事件实时检测

- 实时监控市场 regime 切换：VIX 暴涨（单日 +30%）、波动率聚类（GARCH 检测）、相关性突变（滚动相关系数偏离历史均值 ±2σ）。
- 检测到 regime 切换时，自动触发全局降仓（目标风险暴露降至 50%），并发出高频告警。

### 24. 全成本追踪（Total Cost Analysis）

- 每日生成全成本报告，细分：手续费、滑点、资金费率（加密货币）、税费、机会成本（未成交订单导致的价差损失）。
- 成本数据纳入绩效归因，并在监控看板中可视化。

### 25. 异步对账与指数退避重试

- PositionReconciler 必须支持异步执行（不阻塞交易主循环）。
- 对账失败时，采用指数退避重试策略（1s, 2s, 4s, 8s, 16s，最多 5 次），避免网络抖动误触发 Safe Mode。

### 26. 多策略动态再平衡

- 在 portfolio-001 中明确支持：基于风险贡献（Risk Parity）或 Sharpe 贡献定期（每周）调整各策略资金权重。
- 再平衡操作应最小化冲击成本，支持渐进式调整。

### 27. API Key 使用审计

- 每个 API Key 需关联使用主体（策略 ID 或模块名称），并在审计日志中记录最后一次使用时间、IP、调用次数。
- 提供审计报告：显示每个 Key 的使用频率和最近活动。

### 28. Graceful Shutdown 完整流程

- 系统收到 SIGTERM/SIGINT 信号时，必须执行以下顺序：
  1. 停止接受新订单（标记系统状态为 `shutting_down`）
  2. 取消所有未成交的挂单（调用交易所 cancel_all_orders）
  3. 根据配置决定是否平仓（`close_positions_on_shutdown=true/false`）
  4. 持久化当前状态（内存中的订单、持仓、策略状态写入数据库）
  5. 等待所有后台任务完成（对账、日志刷新），最长等待 30 秒
  6. 释放资源并退出
- 任何步骤失败应记录错误日志并继续执行后续步骤，确保状态一致性。

### 29. 状态恢复机制（State Recovery）

- 系统启动或 crash 后，自动执行恢复流程：
  1. 从数据库读取上次持久化的订单、持仓、策略内部状态
  2. 调用交易所接口查询实际持仓和未决订单
  3. 比对并解决差异：以交易所为准，更新本地状态
  4. 若存在未完成订单，根据市场情况决定撤销或继续等待
  5. 恢复 warm-up 进度（若策略处于 warm-up 期，继续积累数据）
- 恢复完成后发出告警通知，并记录详细恢复日志。

### 30. 多时区与交易时段管理

- config-002 中的 `trading_sessions` 必须支持：加密货币 24/7、A股（9:30-11:30,13:00-15:00）、期货（含夜盘）、美股（美东时间）等。
- 策略信号生成和订单执行必须遵守当前市场的交易时段，非交易时段不产生信号/订单。
- 使用统一的 UTC 时间内部存储，输出时转换为市场本地时间。

### 31. 策略间依赖与互斥

- 在 portfolio-001 中实现策略间互斥规则：例如策略 A 与策略 B 相关性超过 0.7 时，不允许同时满仓，总风险暴露受限。
- 实现信号聚合层：同一方向的多策略信号合并为单一订单，避免重复下单（例如均发出买入信号时只执行一笔）。
- 互斥规则和聚合逻辑均可配置。

### 32. 配置变更审计

- 任何对 `risk.yaml` 或策略参数文件的修改，必须记录前后 diff，并要求审批记录（可通过 Git commit + PR 流程实现）。
- 系统启动时自动检查配置是否与上次批准版本一致，若不一致且无审批记录则拒绝启动。
- **v2.6 增强**：任何 `risk.yaml` 或系统核心配置变更，必须先生成资金影响评估报告（调用 docs-002 工具）并经风控人员审批，审批记录随配置一起存档。

### 33. 回测覆盖率增强

- 在 test-001 中增加 Monte Carlo 异常场景覆盖测试：对 risk 模块（如止损、仓位限制）和 exec 模块（如订单重试、超时）输入随机异常值，验证系统鲁棒性。

### 34. 文档补充非功能性需求

- ARCHITECTURE.md 必须包含“非功能性需求（NFR）”章节，列出：
  - 延迟预算（行情→信号≤10ms，信号→订单≤60ms）
  - 可用性目标（99.9%）
  - RTO（恢复时间目标）≤ 2 小时，RPO（恢复点目标）≤ 1 小时
  - 系统容量（支持同时运行策略数、品种数）

### 35. 全局 API Rate Limit & Quota 管理

- 实现 `GlobalRateLimiter` 类，基于令牌桶或滑动窗口算法，为每个交易所 API 端点独立配置限流阈值（从 config/api.yaml 读取）。
- 所有交易所调用（下单、撤单、查询）必须经过该限流器，超出限制时自动排队或延迟，避免触发交易所封禁。
- 市场波动剧烈时（如 VIX 暴涨），支持动态降低限流阈值（降频至 50%），保护系统稳定性。

### 36. 订单幂等性（Idempotency）

- 每个下单请求必须生成全局唯一的 `client_order_id`（格式：`<strategy_id>_<timestamp>_<random>`），随请求发送至交易所。
- 系统内部维护已使用 ID 的缓存（至少保留 24 小时），重复请求时直接返回原订单状态，禁止重复下单。
- 交易所返回的 `order_id` 与本地 `client_order_id` 双向映射，确保一致性。

### 37. 策略热更新与 A/B 测试框架

- 支持在不停机的情况下，将运行中的策略从一个版本切换到另一个版本（例如从 `v1.0` 到 `v1.1`）。
- 切换时，现有持仓可选择：立即平仓、保留并映射到新策略、或只对新信号生效。
- 提供 A/B 测试模式：将资金分为两组，分别运行新旧策略，实时对比绩效指标（Sharpe、回撤、收益率），并在监控看板展示。
- 所有切换操作需记录审计日志。

### 38. 数据回放一致性保障

- 在 `replay_mode` 中，增加 tick 数据时间戳对齐与去重逻辑：
  - 使用统一时间基准（UTC），将不同数据源的 tick 时间戳对齐到最近的毫秒。
  - 同一毫秒出现多个 tick 时，保留第一个或合并（可配置）。
  - 过滤明显的重复数据（相同价格、数量、时间戳连续出现）。
- 提供数据哈希校验，确保回放数据与原始采集数据一致。

### 39. 监管报告模板系统

- 在 `compliance-001` 中增加可配置的监管模板，支持国内期货/ A 股的大额交易报告、适当性管理要求。
- 模板内容（字段、格式、触发阈值）从配置文件读取，支持多监管区切换。
- 每日自动生成报告，并以 PDF/XML 格式存储，支持手动导出。

### 40. 资源与成本监控

- 在 `monitor-001` 中增加云资源使用量（CPU、内存、存储、网络流量）和 API 调用费用实时监控。
- 设置阈值（例如 CPU > 80% 或月 API 费用 > 预算的 80%），触发告警。
- 每日生成资源/成本报告，发送至运维邮箱。

### 41. 零信任架构增强

- 内部服务间通信必须使用 mTLS 加密，并采用短期 token（有效期 ≤ 1 小时）进行身份验证。
- 服务间调用需携带 trace_id，并记录完整的调用链审计日志。

### 42. 灾难恢复演练自动化

- 在 `ops-001` 中增加每月自动运行的轻量级恢复测试脚本：
  - 模拟数据库崩溃，从最新备份恢复，验证恢复流程成功率。
  - 记录恢复耗时（RTO）和数据损失量（RPO）。
  - 失败时自动告警，并生成详细错误报告。

### 43. 电路熔断器（Circuit Breaker）

- 为每个交易所和核心 API 端点（下单、撤单、查询）独立配置熔断器。
- 熔断器状态：关闭（正常）→ 打开（失败次数超阈值）→ 半开（尝试恢复）。
- 默认阈值：连续失败 5 次，熔断器打开，持续 60 秒后进入半开状态，半开期间成功 3 次则关闭，失败 1 次则重新打开。
- 熔断期间直接快速失败，不调用实际接口，避免雪崩。
- 熔断器状态变化需记录审计日志并告警。

### 44. 策略/系统自诊断与自动恢复

- 实现自诊断引擎（SelfDiagnosticEngine），定期（每 30 秒）检查关键组件：
  - 数据流：最新 tick 时间戳，延迟 > 5 秒告警。
  - 事件总线：队列积压深度 > 1000 告警。
  - 风控引擎：是否正常响应。
  - 交易所连接：心跳超时判断。
- 检测到异常时尝试自动恢复：重启连接、清空队列、切换备用数据源等。
- 若自动恢复失败，则暂停受影响策略，并发出紧急告警。

### 45. 历史订单/成交回放验证

- 提供离线回放工具，使用交易所返回的历史真实成交记录（Order & Fill log）逐笔重放，验证系统决策逻辑与实际结果的一致性。
- 回放应支持三种模式：
  - 决策回放：重新评估信号，对比实际订单。
  - 订单匹配回放：模拟订单生命周期，检查成交是否匹配。
  - 完整回放：包含数据流、信号、风控、执行。
- 回放结果生成差异报告，用于上线前最终验证。

### 46. 多环境配置漂移检测

- CI/CD 流程中增加配置漂移检测步骤：自动对比 dev、test、prod 环境的核心配置文件（如 risk.yaml, system.yaml）。
- 发现差异时输出报告，并阻止部署（除非明确忽略）。
- 支持手动允许已知差异（如不同环境的 API 端点）。

### 47. 事件溯源 + CQRS 模式

- 所有核心状态变更（订单、持仓、风控阈值变更）必须存储为不可变事件。
- 使用事件溯源重建状态，支持任意时间点回放。
- 对读频繁的场景使用 CQRS 分离写入和读取模型，提高性能。

### 48. 性能容量规划（基准测试）

- 在 ARCHITECTURE.md 的 NFR 章节中补充基准容量测试结果：
  - 单节点支持的最大品种数（如 200 个）。
  - 单节点支持的最大策略数（如 20 个）。
  - 系统最大 TPS（每秒订单数，如 1000）。
  - 资源使用基线（CPU/内存/网络）。
  - 基准测试需包含跨区域切换场景下的 RTO/RPO 实测数据
- **v2.11 增强**：包含 Multi-Region Failover 场景下的容量与 RTO/RPO 验证，确保跨区域容灾不影响核心性能指标。

### 49. Canary Release（灰度发布）

- 在 deploy-001 的基础上增加 Canary 发布策略：
  - 先升级 5% 的实例（或一个金丝雀节点），验证 24 小时。
  - 若无异常，逐步扩大比例（10%, 50%, 100%）。
  - 支持自动回滚：若金丝雀节点的错误率或延迟超过阈值，自动回滚所有实例。

### 50. 双重/多重风控校验（风控哨兵）

- 在 OrderManager 发送订单的最终路径上，增加独立的风控哨兵模块（RiskSentinel）。
- 该哨兵与主风险系统（risk-001）解耦，只做最后一道防线检查：
  - 账户可用资金是否足够（含保证金）。
  - 当前日亏损是否已接近熔断阈值（提前预警）。
  - 单品种/总仓位是否超限。
  - 订单价格是否超出合理范围（例如偏离市价过多）。
- 哨兵校验失败时，订单被拒绝，并记录审计日志和告警。
- 哨兵的配置可独立于主风控系统，防止单点失效。

### 51. 订单执行超时兜底（全局未确认订单清理）

- 除了单订单超时撤单机制外，增加全局未确认订单清理器（StaleOrderWatcher）。
- 每 30 秒扫描所有状态为 `SUBMITTED` 或 `PARTIALLY_FILLED` 且超过 `timeout_global`（默认 60 秒）未更新的订单。
- 对超时订单主动向交易所发起查询，确认状态：
  - 若已成交，同步本地状态。
  - 若仍存活且无更新，则发起撤单。
- 清理器记录每次动作，并发出告警（表示网络或交易所可能异常）。

### 52. 回测引擎插件化抽象层

- 在 backtest-001 的基础上，增加统一的 `BacktestEngine` 抽象接口。
- 支持插件化注册：用户可轻松接入自定义高性能引擎（如 Rust 实现的引擎、C++ 扩展、DolphinDB 等），只需实现必要方法（`run`, `get_indicators`, `get_trades` 等）。
- 内置引擎（vectorbt-pro, backtrader）作为默认实现，新引擎可动态加载。

### 53. 绩效归因细化（Brinson 模型）

- 在 portfolio-001 中增加四维度归因分析：
  - 风格归因（价值/成长/大小盘）。
  - 行业/因子归因（如 momentum, volatility）。
  - 时机归因（市场择时能力）。
  - 选股归因（个股超额收益）。
- 使用 Brinson 模型或类似方法，输出每日/每周归因报告，并在监控看板展示。

### 54. 压力测试自动化（CI/CD 集成）

- 在 CI/CD 流水线中增加定期（如每周）运行压力测试的步骤。
- 压力测试包括：
  - Monte Carlo 模拟（1000 次随机采样）。
  - 历史极端场景组合（2020.3、2015.8、2022 熊市等）。
  - 同时运行，生成综合压力测试报告（最大回撤、最大杠杆、流动性缺口）。
- 若压力测试结果突破预设阈值（例如回撤 > 25%），则 CI 标记为警告但不阻塞部署（可配置）。

### 55. 用户/权限分级（RBAC）

- 支持多角色权限控制：
  - 运维（部署、监控、重启）。
  - 交易员（策略启停、参数调整、手动下单）。
  - 风控（修改风控参数、查看风险报告）。
  - 审计（只读所有日志和报告）。
- 使用基于角色的访问控制（RBAC），通过配置文件或内部服务管理。
- 所有操作记录审计日志，敏感操作需二次审批。

### 56. 密钥/凭证自动轮换集成（零停机）

- 实现完全自动化的 API Key 轮换流程：
  - 定期（每 60 天）触发 Vault 中的密钥更新，生成新 Key 并标记为待生效。
  - 新 Key 灰度验证：先在模拟盘中使用 24 小时，确认无误后再切换至生产。
  - 切换时支持零停机热更新：系统通过配置热重载机制加载新 Key，无需重启。
  - 轮换后自动调用交易所验证端点，确保新 Key 有效且权限正确。
  - 旧 Key 保留 7 天作为回退，然后自动删除。
- 所有轮换操作记录审计日志，失败时告警并保留旧 Key。

### 57. 跨市场结算周期处理

- 在 risk-001 和 exec-001 中增加结算窗口风控逻辑：
  - 为每个市场配置结算时间（A 股：T+1 16:00；期货：当日 15:30；加密货币：实时）。
  - 在结算前 30 分钟，系统自动进入 **结算窗口模式**：限制新开仓（可配置禁止或降低仓位），仅允许平仓。
  - 结算完成后的首个数据更新点，重新计算可用资金和持仓，恢复正常交易。
  - 结算窗口内的订单风险独立监控，防止因资金未到账导致透支。

### 58. 异常订单自动重试策略

- 对于状态为 `REJECTED`, `CANCELLED`, `EXPIRED` 的订单，根据配置自动重试：
  - 可配置最多重试次数（默认 3 次），重试间隔递增（1s, 2s, 4s）。
  - 仅对可重试的错误码生效（如 `INSUFFICIENT_BALANCE` 不重试，`CONNECTION_TIMEOUT` 重试）。
  - 每次重试记录原因，并在最终失败时生成告警和归因分析（区分是价格不合理、资金不足还是系统问题）。
- 重试逻辑与订单幂等性结合，避免重复下单。

### 59. 系统级容量与限流动态调整

- 结合 monitor-001 的资源监控（CPU、内存、网络 IO）动态调整全局限流器阈值：
  - 当 CPU > 80% 或内存 > 90% 持续 5 分钟，自动将 GlobalRateLimiter 的每秒请求数上限降低 50%。
  - 若资源持续紧张，进一步暂停非核心策略（如低频辅助策略）。
  - 资源恢复正常后，逐步恢复限流阈值和策略。
- 所有调整记录审计日志，并在监控看板显示当前限流状态。

### 60. 蓝绿部署 + Canary 自动化决策

- 在 deploy-001 基础上增加基于监控指标的自动决策引擎：
  - Canary 阶段实时监控金丝雀实例的错误率、P99 延迟、策略 Sharpe 等指标。
  - 若错误率 > 5% 或延迟 > 200ms 或 Sharpe 下降 > 30%，自动触发回滚。
  - 若 24 小时内无异常，自动将流量扩大至 100%。
- 回滚操作自动执行（切换负载均衡器指向旧版本），并发送告警。

### 61. 多租户数据隔离

- 在 db-001 中实现行级或表级数据隔离：
  - PostgreSQL 中为每个租户（账户/策略组）使用独立的 schema 或添加 `tenant_id` 行级策略（RLS）。
  - ClickHouse 中使用分区键 `tenant_id` 隔离数据。
- 应用层所有查询自动注入租户上下文，防止跨租户数据泄露。
- 迁移脚本支持多租户模式。

### 62. 文档版本化

- ARCHITECTURE.md 和 RISK_POLICY.md 增加版本历史和变更记录：
  - 文档顶部增加版本表格（版本号、日期、变更作者、变更摘要）。
  - 使用 Git 管理文档，每次修改必须更新版本号并记录变更。
  - CI 中检查文档版本是否与最后一次提交一致，防止未记录的修改。

### 63. 订单簿深度缓存与快照一致性

- exec-003 中的订单簿重建模块必须支持本地 L2 快照持久化（每 10 秒或每次变化时保存）。
- 系统启动时优先从本地缓存加载最近的订单簿快照，然后通过 WebSocket 增量更新，避免冷启动时使用过期数据。
- 快照版本号与交易所同步，不一致时丢弃本地缓存，重新获取全量快照。

### 64. 全链路延迟监控与 SLO 告警

- monitor-001 增加端到端延迟追踪：从行情 tick 进入系统到对应订单成交的时间差（P50, P95, P99）。
- 设置 SLO 目标：P99 ≤ 150ms，突破时自动告警并关联 trace_id 定位瓶颈环节。
- 延迟指标纳入监控看板，并按策略/品种/交易所维度聚合。

### 65. 策略参数变更影响评估

- 新增策略参数变更前的自动化影响评估流程：
  - 对拟修改的参数（如均线周期、止损比例）自动运行短期 Walk-Forward 回测（最近 3 个月）。
  - 输出变更前后 Sharpe、MDD、胜率等关键指标的变化百分比。
  - 生成变更风险报告（包含建议：是否允许上线），该报告需风控人员确认后方可生效。
- 评估结果记录审计日志。

### 66. 数据源优先级与自动降级

- data-001 为每个市场配置数据源优先级列表（例如 A 股：tushare-pro → akshare → yfinance）。
- 主数据源连续失败 3 次或延迟超过阈值时，自动降级到下一优先级数据源，切换过程对上层策略透明。
- 记录切换日志（时间、原因、切换后的数据源），并发出告警。

### 67. 量化指标标准化

- docs-001 中增加“绩效指标标准库”章节，明确定义以下指标的计算公式（避免不同引擎差异）：
  - 年化收益率、夏普比率（无风险利率取值）、最大回撤、卡玛比率、胜率、盈亏比、波动率。
- 所有回测和实盘监控使用同一套标准实现，并在代码中提供单元测试验证计算正确性。

### 68. 审计日志查询工具

- security-002 增加简易 Web 查询界面（只读），支持按时间、用户、操作类型、策略 ID 等条件搜索审计日志。
- 工具需进行身份验证，仅限风控和审计角色访问。
- 查询结果支持导出为 CSV。

### 69. 核心状态机与 Invariant 强制执行引擎（v2.3 新增）

- 实现独立的 **Unified State Machine & Invariant Enforcer** 模块（对应 feature `core-002`），管理所有核心实体（订单、持仓、权益）的有限状态转换。
- 每个关键操作（下单、成交、撤单、风控触发）后，自动断言所有核心 Invariants（Position, Equity, Order Idempotency, Data Consistency, Risk Limit）。
- 若任何 Invariant 被打破，系统立即进入 Safe Mode，阻断所有新交易，并触发最高优先级告警。
- 支持事件溯源，可回放历史事件重建任意时刻的状态，用于审计和调试。
- **State Machine 是所有核心模块的唯一入口，禁止绕过**。

### 70. 基准比较与超额收益归因体系（v2.3 新增）

- 新增 `portfolio-002` 模块，支持多基准指数（HS300, SPX, BTC 等）的自动下载和跟踪。
- 每日计算组合相对于每个基准的 Information Ratio、Tracking Error、Active Share。
- 超额收益归因：基于 Brinson 模型分解为资产配置、行业选择、个股选择等部分，生成每日/每周报告。

### 71. 跨市场统一资金与保证金管理（v2.3 新增）

- 新增 `risk-003` 模块，统一管理多交易所账户的保证金、杠杆、购买力。
- 实时合并计算总风险敞口（总杠杆、总保证金占用、可用资金），支持不同市场规则（期货维持保证金、股票 T+2、加密货币即时结算）。
- 动态调整总杠杆上限，防止超限，提供保证金监控看板和预警。

### 72. 全局审计事件总线与事件溯源核心（v2.4 新增）

- 新增 `core-003`：Global Audit Event Bus & Event Sourcing Core。
- 所有重要操作（订单、风控决策、配置变更、Invariant 违反、状态转换）必须统一发布到该事件总线。
- 事件总线为不可变、追加写的核心基础设施，保证“一次写入，多处使用，不可篡改”。
- 所有审计日志、监控指标、恢复重建必须从事件总线消费生成，不再从各模块直接写日志。

### 73. 资金影响评估模板标准化与自动生成工具（v2.4 新增）

- 在 `docs-002` 中定义标准化的资金影响评估模板，包含：
  - 最大潜在亏损（基于 VaR 和压力测试）
  - 流动性冲击估算（持仓 vs 市场深度）
  - 极端情景损失（市场单日 -10%、流动性枯竭等）
  - Invariant 风险分析（变更可能打破的不变性）
  - 建议的缓释措施
- `exec-001`、`portfolio-001`、`risk-001` 等高风险模块在每次重大变更后必须自动调用工具生成该评估报告并存档。
- 报告需风控人员审批后方可上线。

### 74. 动态压力测试场景生成器（v2.4 新增）

- 新增 `risk-004`：基于当前市场 regime（波动率、相关性、流动性）自动生成动态压力测试场景。
- 场景包括：历史重现、统计合成、极端尾部事件等。
- 每天凌晨自动运行，生成当日风险报告并推送。

### 75. Invariant 违反场景的自动化混沌测试（v2.4 新增）

- 新增 `test-006`：专门针对 Invariant 违反的混沌测试套件。
- 故意注入违反 Invariants 的事件（如模拟网络延迟导致仓位对账失败），验证系统能否正确进入 Safe Mode 并记录事件。
- 保证 Invariant 强制引擎的可靠性。

### 76. 数据 Schema 版本管理与兼容性强制校验（v2.5 新增）

- 新增 `data-004`：Data Schema Versioning & Compatibility Engine。
- 定义数据 Schema 版本（例如 `v2026.05`），所有数据表必须声明版本。
- 回测/实盘启动时，强制校验策略所需 schema 与当前数据 schema 的兼容性（通过版本匹配表）。
- 不兼容时拒绝运行并告警，并提供自动迁移脚本（如果可能）。
- 防止因字段新增/删除导致的静默回测漂移。

### 77. 多策略/多账户资金防火墙与动态分配引擎（v2.5 新增）

- 新增 `portfolio-003`：Global Capital Firewall & Dynamic Allocator。
- 维护全局可用资金池，实时扣减/释放（基于策略的已用资金 + 挂单冻结）。
- 单策略/单账户最大占用比例动态调整（基于策略健康度 + VaR 贡献，高风险策略自动降低上限）。
- 极端情况下（如策略连续亏损或 Invariant 违反），可自动将问题策略完全隔离（资金归零，平仓所有持仓）。
- 所有分配和隔离操作记录审计日志。

### 78. 多监管区规则引擎（v2.5 新增）

- 在 compliance-001 中增加 Regulatory Rule Engine，支持配置文件切换监管模式（中国、美国、香港、加密）。
- 每个监管模式定义：报告格式（PDF/XML）、杠杆上限、大额交易阈值、持仓披露规则等。
- 每日根据当前监管模式自动生成对应格式的报告，并通过邮件/API 提交。
- 监管模式变更需经合规人员审批。

### 79. 全链路资金影响评估自动化验证（v2.5 新增）

- 新增 `test-007`：在 CI/CD 中自动运行资金影响评估验证。
- 对于每个重大变更（涉及 exec, risk, portfolio 模块），自动调用资金影响评估工具生成报告，并与变更前基准对比。
- 若评估结果显示潜在亏损增加超过 10% 或 Invariant 风险升高，则 CI 标记为警告，需人工确认后才能合并。

### 80. 策略/模型下线机制（v2.5 新增）

- 所有策略必须有明确的下线流程：
  - 先停止产生新信号（标记为 `deprecated`）。
  - 平仓所有关联持仓（可设置时间窗口渐进平仓）。
  - 释放占用的资金，并转移策略状态至归档数据库。
  - 下线完成后，从生产环境中移除策略代码和配置。
- 下线操作需经过风控审批，并记录审计日志。

### 81. 模型风险专用管理框架（Model Risk Management）（v2.6 新增）

- 新增 `risk-005`：Model Risk Management Framework。
- **模型漂移监控**：实时监控模型预测分布、特征分布与训练时的差异（PSI、特征漂移），超过阈值告警。
- **概念漂移检测**：使用统计检验（如 ADWIN）检测模型输入输出关系变化。
- **数据分布偏移监控**：对比实时数据与训练数据的统计特征。
- **影子测试**：新模型版本与当前生产模型并行运行，比较决策差异和绩效影响。
- **模型贡献度风险归因**：与 portfolio-002 结合，分析模型对组合风险（VaR、回撤）的贡献。
- **模型失效应急预案**：当模型性能严重下降（如 Sharpe 连续下降 30%）时，自动切换到备用规则策略（如双均线），并告警。

### 82. 策略全生命周期管理标准化（v2.6 新增）

- 在 `portfolio-001` 中增加 **Strategy Lifecycle Manager** 子模块。
- 定义清晰的状态机：
  - **Developing**（开发）：仅限回测，未分配真实资金。
  - **Paper**（模拟盘）：使用模拟资金运行，验证稳定性。
  - **Small Live**（小资金实盘）：使用 ≤1% 总资金。
  - **Full Live**（全资金）：正常运行。
  - **Deprecated**（已废弃）：停止新信号，平仓中。
  - **Archived**（已归档）：完全下线，数据仅用于回顾。
- 状态转换必须经过风控审批，并记录审计日志。
- 每个状态对应的资金上限、风险限制由全局配置文件定义。

### 83. 全局配置变更影响评估闭环（v2.6 补充）

- 第 32 条已明确：任何 `risk.yaml` 或系统核心配置变更，必须先生成资金影响评估报告并经风控审批。
- 该报告自动调用 `docs-002` 工具生成，并与配置 diff 一起提交审批。
- 审批通过后配置方可生效，否则拒绝变更。

### 84. 全局 Kill Switch / 紧急总闸机制（v2.7 新增）

- 新增 `risk-006`：Global Kill Switch & Emergency Override。
- 提供最高级别紧急总闸：一键全部平仓 + 冻结所有新交易（包括所有策略、所有市场）。
- 支持两种触发方式：
  - **人工触发**：通过 Web 控制台、移动端紧急按钮、API 调用。
  - **自动触发**：当系统检测到极端黑天鹅事件（如市场暴跌超过预设阈值、交易所异常公告、合规突发事件）时自动激活。
- Kill Switch 激活后，系统状态变为 `EMERGENCY`，阻断所有新订单，强制平仓所有持仓，并冻结所有账户的资金配置。
- **Kill Switch 优先级高于所有其他执行路径，是系统最高优先级指令。**
- **Kill Switch 必须在所有核心路径（包括 State Machine、Event Bus、Portfolio Allocator）中被硬编码为最高优先级处理。**
- 恢复需要人工双重确认（风控总监 + 运维总监）才能解除。
- 所有 Kill Switch 操作必须记录到独立的、不可篡改的审计日志中。

### 85. 策略/模型代码完整性校验与签名（v2.7 新增）

- 在 `security-001` 中增强：策略代码签名 + CI/CD 签名验证流程。
- 所有策略代码和模型文件在生产部署前必须使用私钥进行数字签名。
- CI/CD 流水线在部署前强制验证签名，未通过签名验证的代码禁止上线。
- 签名密钥由安全团队保管，策略更新必须经过签名审批流程。
- 运行时系统定期检查已加载策略的签名有效性，防止运行时篡改。

### 86. 全局系统版本清单与依赖快照机制（v2.8 新增）

- 新增 `core-004`：System Manifest & Full Dependency Snapshot（优先级较低，但对长期可重现性极重要）。
- 生成系统顶层版本清单，包含：所有组件版本（Python、数据库、中间件）、关键配置哈希、策略代码签名哈希、数据 schema 版本等。
- 每次部署时自动生成清单，并与运行时代码绑定。
- 支持通过命令行或 API 查询当前运行系统的完整版本清单。
- 用于问题回溯、合规审计和灾难恢复时的版本一致性验证。

### 87. 监管/审计长期存档不可变性（v2.8 新增）

- 在 `compliance-001` 中增加对监管报告和审计日志的长期存档要求：
  - 所有监管报告和审计日志必须使用 WORM（Write Once Read Many）存储（如 AWS S3 Object Lock、Azure Immutable Blob）。
  - 或采用区块链级时间戳证明（如将报告哈希上链）。
  - 保留期限根据监管要求配置（默认 7 年），到期前自动提醒归档。
  - 存档数据禁止任何形式的修改或删除（除非司法授权）。

### 88. Kill Switch 全链路集成验证（v2.8 强化）

- 在 `test-010` 中增加全系统端到端 Kill Switch + 灾难恢复联合压力测试。
- 验证 Kill Switch 在 Graceful Shutdown、State Machine、Event Bus、Portfolio Firewall 等所有关键路径上的触发与响应正确性。
- 测试包括：模拟 Kill Switch 激活后，所有模块立即进入 Emergency 状态，订单被阻断，持仓被平仓，资金被冻结。
- 恢复后系统状态必须完全一致。

## 每次会话结束（必须执行）

1. **Git 提交**（格式要求）：
   - `feat: [feature-id] - 简要描述`
   - `fix: [feature-id] - 简要描述`
   - `refactor: [feature-id] - 简要描述`
2. 更新 `feature_list.json` 中对应 `passes` 字段（**仅在通过完整验证后**允许设为 true）
3. 在 `claude-progress.txt` 追加本次会话摘要：
   - 完成的功能及关键指标变化
   - 遗留风险（如有）
   - 下一步建议
4. 确保 `./init.sh` 在当前环境下仍可正常运行（支持 `--force-rebuild` 和 `--clean` 参数）
5. 运行一次 Smoke Test（快速回测 + 风控检查）确认没有引入新问题

## 绝对禁止事项（红线）

- 未经完整回测和风控验证就标记 `passes: true`
- 把未经验证的代码合并到 `main` 分支
- 硬编码任何密钥、URL 或关键阈值
- 在实盘账户开启状态下随意修改策略参数（必须走 CI/CD）
- 删除或大幅修改 `feature_list.json` 中的验证步骤
- 在没有 git commit 的情况下结束会话
- 忽略最大回撤、资金曲线安全、核心 Invariants 等核心风险指标

**文档版本历史**

- v2.11 (2026-05-18)：在第 48 条（性能容量规划）中补充 Multi-Region Failover 容量与 RTO/RPO 验证要求。完善最终稳定版声明：v2.11 为最终稳定版，后续任何修改必须经过风控审批并记录为 patch。
- v2.10 (2026-05-15)：在最高原则中增加 Kill Switch/Invariant/Code Signing 双签要求；在第2条末尾增加部署 artifacts 签名验证；在 Kill Switch 描述中增加硬编码最高优先级要求。
- v2.9 (2026-05-15)：整合了 v2.8 的完善内容并标注最终稳定版。
- v2.8 (2026-05-15)：新增规则 86、87、88（全局系统版本清单、监管存档不可变性、Kill Switch 全链路验证）。
- v2.7 (2026-05-15)：新增规则 84、85（全局 Kill Switch、策略代码签名）
- v2.6 (2026-05-15)：新增规则 81-83
- v2.5 (2026-05-15)：新增规则 76-80
- v2.4 (2026-05-15)：新增规则 72-75
- v2.3 (2026-05-15)：新增规则 69-71
- v2.2 (2026-05-15)：新增核心 Invariants、优先级权重矩阵、Corporate Actions 与多币种 FX 风险要求
- v2.1 (之前版本)：原生产级规范

**维护要求**：任何对本文件的修改必须更新版本号并记录变更历史。v2.11 为最终稳定版，后续仅做 bugfix、环境适配、安全补丁或特定市场合规要求，任何超出此范围的修改（包括新增核心规则或重大架构变更）必须经过风控委员会审批并标注为 major 版本。所有 patch 级别的修改必须记录在案，并在下次发布时汇总。